---
id: HML-013.01
title: Convert polis agents from markdown to canonical TOML format
status: Done
assignee: []
created_date: '2026-08-08 16:59'
updated_date: '2026-08-08 17:05'
labels: []
dependencies: []
parent_task_id: HML-013
priority: high
type: task
ordinal: 11000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Convert existing markdown agent files to canonical TOML format (agent.toml + system-prompt.md).

## Current Format
```
agents/
├── ag-quick-chat.md
├── ag-blog-writer.md
├── ag-cisco-meraki-automation.md
└── ag-linear-tickets.md
```

## Target Format
```
agents/
├── ag-quick-chat/
│   ├── agent.toml
│   └── system-prompt.md
├── ag-blog-writer/
│   ├── agent.toml
│   └── system-prompt.md
etc...
```

## Steps
1. Create conversion script that:
   - Parses YAML frontmatter from .md files
   - Converts to TOML format
   - Splits body into system-prompt.md
2. Run conversion on all agent files
3. Test one agent manually with `builtins.fromTOML`
4. Commit to polis git repo
5. Update workstation clone (git pull)

## Files Affected
- polis/agents/*.md → polis/agents/*/agent.toml + system-prompt.md
- Create: polis/scripts/convert-md-to-toml.sh
<!-- SECTION:DESCRIPTION:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Successfully converted all 4 agents from markdown format to canonical TOML format.

## What Was Done

**Conversion Script Created:**
- `scripts/convert-md-to-toml.py` - Python script using PyYAML and toml packages
- Parses YAML frontmatter from `.md.bak` files
- Converts permissions to canonical format (intent + rules)
- Splits body into `system-prompt.md`

**Agents Converted:**
1. `ag-quick-chat` - Quick chat agent for fast-flowing conversations
2. `ag-blog-writer` - Blog post writing agent
3. `ag-cisco-meraki-automation` - Cisco Meraki automation agent
4. `ag-linear-tickets` - Linear ticket creation agent

**Structure Created:**
```
agents/
├── ag-quick-chat/
│   ├── agent.toml         (metadata with permissions)
│   └── system-prompt.md   (prompt body)
├── ag-blog-writer/
│   ├── agent.toml
│   └── system-prompt.md
etc...
```

**Verification:**
- ✅ All agent.toml files parse with `builtins.fromTOML`
- ✅ System prompts extracted correctly
- ✅ Permissions converted to canonical format (intent + rules)
- ✅ Committed and pushed to polis repo

**Example TOML Output:**
```toml
name = "ag-quick-chat"
description = "Quick chat agent for fast-flowing conversations"
mode = "primary"

[permissions.skill]
intent = "ask"
rules = [ "sk-quick-convo-mode:allow" ]
```

## Next Steps

Ready for **HML-013.02**: Implement polis/lib/default.nix with loadAgents using builtins.fromTOML
<!-- SECTION:FINAL_SUMMARY:END -->
